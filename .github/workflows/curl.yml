
name: POC | MCK
on:
on:      
  workflow_dispatch:
    inputs:
      restart-controllers:
        type: boolean
        required: true
        default: false

jobs:
  First_Workflow:
    name: "Test"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Running Commands
        run: |
          ls -al
          pwd
          echo $GITHUB_REF_NAME
          # echo "release_version=`$(echo $GITHUB_REF_NAME) |  ` >> $GITHUB_ENV

